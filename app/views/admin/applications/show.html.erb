<!DOCTYPE html>
<html>
  <head>
    <title>App Review</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Bootstrap -->
    <link href="bootstrap/css/bootstrap.min.css" rel="stylesheet">
    <!-- styles -->
    <link href="css/styles.css" rel="stylesheet">

    <!-- HTML5 Shim and Respond.js IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
      <script src="https://oss.maxcdn.com/libs/respond.js/1.3.0/respond.min.js"></script>
    <![endif]-->
  </head>
  <body>

    <div class="page-content">
    	<div class="row">
		    <%= render "admin/shared/wrap" %>
	  		<div class="col-md-6">
	  			<div class="content-box-large">
	  				<div class="panel-heading">
						<div class="panel-title"><%= @application.application_name %></div>
            <%= image_tag(@application.appimage.url, :width => 100, :height => 100) if @application.appimage? %>
						
						<div class="panel-options">
							<a href="#" data-rel="collapse"><i class="glyphicon glyphicon-refresh"></i></a>
							<a href="#" data-rel="reload"><i class="glyphicon glyphicon-cog"></i></a>
						</div>
					</div>
	  				<div class="panel-body">
              <div class="panel-body">
                <%= raw(@application.description) %>
              </div>
              <div class="panel-body">
                <%= @application.team %>
              </div>
              <div class="panel-body">
                <%= @application.device %>
              </div>
              <p>
                <%= link_to "Delete", admin_application_path, method: :delete, data: { confirm: "You sure?"}, class: "btn btn-danger" %>
              </p>
              <p>
                <%= link_to "Edit", edit_admin_application_path, class: "btn btn-success" %>
              </p>
              
	  				</div>
            <div class="panel-body">
              <p>
                <h6>Categories</h6>
                <% @application.categories.each do |category| %>
                  <%= link_to category.category_name,  admin_category_path(category) %>
                <% end %>
              </p>
            </div>
	  			</div>
	  		</div>
  		</div>
	</div>


    <!-- jQuery (necessary for Bootstrap's JavaScript plugins) -->
    <script src="https://code.jquery.com/jquery.js"></script>
    <!-- Include all compiled plugins (below), or include individual files as needed -->
    <script src="bootstrap/js/bootstrap.min.js"></script>
    <script src="js/custom.js"></script>
  </body>
</html>